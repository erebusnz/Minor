# Morris (C port) has NO required third-party dependencies for the default
# `cli` backend: it shells out to the `claude` CLI and uses only the stdlib.
#
# The optional API backends need a provider SDK. Install the one you want:
#
#     pip install anthropic   # --backend api,    uses ANTHROPIC_API_KEY
#     pip install openai      # --backend openai, uses OPENAI_API_KEY
#
# anthropic>=0.40
# openai>=1.0
